services:
  frontend:
    image: akta2910/reqnest-frontend:latest
    container_name: reqnest-frontend
    ports:
      - "8081:80"
    environment:
      - VITE_API_URL=${VITE_API_URL}
    networks:
      - cloudnet

networks:
  cloudnet:
    driver: bridge
